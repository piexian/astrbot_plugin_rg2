{
  "timeout_seconds": {
    "description": "游戏超时时间（秒），装填后等待开枪的时间限制",
    "type": "int",
    "default": 120,
    "hint": "建议设置60-300秒，超时后游戏自动结束"
  },
  "misfire_probability": {
    "description": "随机走火概率（0.0-1.0），群聊中每条消息的触发几率",
    "type": "float",
    "default": 0.003,
    "hint": "设置为0关闭走火，0.003=0.3%概率，建议0.001-0.005"
  },
  "min_ban_seconds": {
    "description": "最小禁言时长（秒），中弹后的基础惩罚时间",
    "type": "int",
    "default": 60,
    "hint": "最低禁言时间，必须小于最大禁言时长"
  },
  "max_ban_seconds": {
    "description": "最大禁言时长（秒），中弹后的最高惩罚时间",
    "type": "int",
    "default": 300,
    "hint": "最高禁言时间，必须大于最小禁言时长"
  },
  "misfire_enabled_by_default": {
    "description": "新群聊默认是否开启随机走火功能",
    "type": "bool",
    "default": false,
    "hint": "开启后新群第一次使用会自动启用走火功能"
  },
  "command_prefix": {
    "description": "指令前缀，用于识别指令消息",
 "type": "str",
    "default": "/",
    "hint": "默认为/，可设置为!、.等其他符号，避免随机走火误触发"
  }
}